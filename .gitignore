node_modules/
client/node_modules/
client/build/
data/
logs/
.env
.DS_Store
*.log
.vscode/
.idea/
# Remove sensitive config data (keep structure only)
data/config.json
data/shift_logs.db
# Remove log files
logs/*.txt
# Python standalone server generated files
server_config.json
build/
# dist/ - Now tracked in git for distribution
# But allow everything inside dist/ to be committed
!dist/
*.spec
# Node.js portable (should be added manually)
nodejs/
# But allow nodejs inside dist/
!dist/nodejs/

